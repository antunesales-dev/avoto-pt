# 1) Assets: reescrever para o mesmo path.
#    Se o ficheiro não existir → 404 (não index.html).
#    Evita "Failed to fetch dynamically imported module" após deploys.
/assets/*  /assets/:splat  200

# 2) SPA (Vue Router history): resto das rotas → index.html
/*  /index.html  200
