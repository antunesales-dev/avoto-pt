# Sync diário: AR (Node) + despesa/digest (edge)
name: Sync diário (AR + despesa + digest)

on:
  schedule:
    # 06:30 UTC
    - cron: '30 6 * * *'
  workflow_dispatch:
    inputs:
      ar_limit:
        description: 'Máx. iniciativas AR a upsert'
        default: '200'
        required: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      # Sem "version": usa package.json "packageManager" (pnpm@10.32.1)
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: AR sync (Node — JSON grande)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: node scripts/sync-ar.mjs --limit=${{ github.event.inputs.ar_limit || 'all' }}

      - name: despesa-sync (edge, paginado)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          AVOTO_CRON_SECRET: ${{ secrets.AVOTO_CRON_SECRET }}
        run: |
          set -e
          curl -fsS -X POST "$SUPABASE_URL/functions/v1/despesa-sync?limit=400" \
            -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
            -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
            -H "x-avoto-cron-secret: $AVOTO_CRON_SECRET" \
            -H "Content-Type: application/json" -d '{}'
          echo

      - name: Digests multi-secção (todas as datas com dados)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: node scripts/generate-digests.mjs
